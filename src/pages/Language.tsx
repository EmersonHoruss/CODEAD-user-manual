import { optionsWithIcons } from "../components/sidenav/constants";
import PageTemplate from "./PageTemplate";

const Language = () => {
  return (
    <PageTemplate option={optionsWithIcons.language}>lorem*100</PageTemplate>
  );
};

export default Language;
